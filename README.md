Simple test of protobuffers with dart client and nodejs server.

In order to regenerate models and rpc you need to install:

for dart:

```
pub global activate protoc_plugin
```

for node:

```
npm install -g grpc-tools
```
